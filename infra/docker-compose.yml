services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: zbot
    ports:
      - "5432:5432"
    volumes:
      - zbot_pg_data:/var/lib/postgresql/data

volumes:
  zbot_pg_data:
